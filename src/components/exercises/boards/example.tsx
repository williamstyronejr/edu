import * as React from 'react';
import Game from '../../shared/Game';

const Tutorial = (props: Object) => {
  return <Game>{/* Put game objects here to create a board */}</Game>;
};

export default Tutorial;
